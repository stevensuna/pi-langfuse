node_modules/
bun.lock
*.log
.DS_Store
config.json
/.pi-lens
dist/
.npmrc
.pi/

# The extension loads committed dist/ output directly. Keep its runtime modules
# visible to Git while all other generated files stay ignored.
!dist/
dist/*
!dist/diagnostics.*
!dist/model-pricing.*
